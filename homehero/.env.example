# HomeHero Environment Variables

# Server port (default: 3000)
PORT=3000

# Environment mode (development, production, test)
NODE_ENV=production

# JWT Secret for authentication tokens
# If not set, a random secret is auto-generated and persisted to /data/.jwt-secret
# For distributed deployments, set this to ensure consistent tokens across restarts
# JWT_SECRET=your-secure-random-secret-here

# Data directory for database and persistent files
# Default: /data (Home Assistant) or ./data (development)
# DATA_DIR=/data

# Database file path (overrides DATA_DIR if set)
# Default: ${DATA_DIR}/homehero.db
# DB_PATH=/data/homehero.db
